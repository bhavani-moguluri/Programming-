include <stdio.h>

void FindArea(float x, float y) {
    float area = x * y;
    printf(" %f", area);
}

int main() {
    float length, width;
    scanf("%f", &length);
    scanf("%f", &width);
    FindArea(length, width);
    return 0;
}
