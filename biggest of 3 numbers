#include<studio.h>
int big(int,int,int);
main()
{
int a,b,c,h;
scanf("%d%d%d",&a,&b,&c);
h=big(a,b,c);
printf("biggest=%d",a);
}
int big(int a ,int b ,int c)
{
if(a>b&&a>c)
return a;
else if (b>a&&b>c)
return b;
else
return c;
}
